import styled, { css } from 'styled-components'

export const thumbSize = 30

const absoluteFromBottom = 8
export const hiddenBarPosition = css`
    bottom: ${absoluteFromBottom}px;
`
export const progressBarPosition = css`
    bottom: ${absoluteFromBottom}px;
`